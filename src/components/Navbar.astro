---
import "../styles/global.css";
import LanguageToggle from "./LanguageToggle.astro";
---
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>About PADK</title>

  <style>
    .hero-gradient {
      background: linear-gradient(180deg, rgba(255,255,255,0.9), rgba(255,255,255,0.9));
    }
    .cookie-banner {
      backdrop-filter: blur(4px);
    }
  </style>
</head>
<body class="antialiased text-gray-800 bg-white">

  <!-- NAV -->
  <header class="border-b">
    <div class="max-w-6xl mx-auto px-6 py-4 flex items-center justify-between">
      <a href="/" class="text-xl font-semibold">PADK</a>
      <nav class="hidden md:flex gap-6 text-sm items-center"> 
        <a class="hover:underline font-semibold" href="/about-us">ABOUT</a>
        <a class="hover:underline" href="/resources">RESOURCES</a>
        <a class="hover:underline" href="/projects">PROJECTS</a>
      <LanguageToggle/>
      </nav>

      <div class="md:hidden">
        <!-- mobile menu toggle placeholder -->
        <button aria-label="open menu" class="p-2 rounded bg-gray-100">
          ☰
        </button>
      </div>
    </div>
  </header>