---
import "../styles/global.css";

const {title} =  Astro.props;

---
<section class="hero-gradient bg-gray-200">
	<div class="max-w-6xl mx-auto px-6 py-12">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 items-center">
        <div class="md:col-span-2">
          <slot></slot>
			<h1 class="text-3xl md:text-4xl font-bold leading-tight mb-4">{title}</h1>

          <p class="text-lg text-gray-700 mb-6">
            Partner with us — let us show you how.
          </p>

          <div class="flex gap-3">
            <a href="#help" class="inline-block px-5 py-3 bg-green-600 text-white rounded shadow-sm hover:bg-green-700">Partner with us</a>
            <a href="/contact" class="inline-block px-5 py-3 border border-gray-300 rounded hover:bg-gray-100">Contact</a>
          </div>
        </div>

        <!-- Hero image column -->
        <div class="flex items-center justify-center">
          <!-- Replace src with actual image from padk.org if you want exact match -->
          <img src="https://via.placeholder.com/360x260.png?text=PADK+image" alt="PADK formation" class="rounded shadow-md object-cover w-full h-56 md:h-60"/>
        </div>
      </div>
    </div>
  </section>